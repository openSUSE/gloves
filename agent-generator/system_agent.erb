$:.unshift(File::join("../../..", "dbus-infrastructure"))

require 'dbus_services/<%= type.to_s.downcase %>file_service'

module SystemAgents
  class <%= agent_class %> < DbusServices::<%= type.to_s.capitalize %>Service

    # identification of relevant DBUS service
    filename "<%= identifier %>"

<% if type == :file %>
    def read(params)
      #TODO add your code here
      return {}
    end

    def write(params)
      #TODO add your code here
      return {}
    end
<% elsif type == :script %>
    def execute(params)
      #TODO add your code here
      return {}
    end
<% else
    raise "unknown type"
   end %>
  end
end
